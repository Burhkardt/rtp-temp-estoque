services:
  backend:
    build:
      context: ./backend
    container_name: rtp-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./backend:/app/backend
    environment:
      - FLASK_ENV=development
    restart: always


